#!/usr/bin/env ruby

begin
  require 'sinatra_deployer/app.rb'
rescue LoadError => e
  require 'rubygems'
  path = File.expand_path '../../lib', __FILE__
  $:.unshift(path) if File.directory?(path) && !$:.include?(path)
  require 'sinatra_deployer/app.rb'
end
